// Archon Systems — fill these in once, everything on the site reads from here.
// The "Book a meeting" CTA now opens the intake modal (src/components/IntakeModal),
// which writes to Firestore — no calendar link needed.
export const SITE = {
  name: 'Archon Systems',
  tagline: 'AI systems for businesses',
  // Studio mailbox pending — leave empty until it exists (no personal accounts
  // on the site); the Close section hides the mailto line when this is blank.
  email: '',
}
